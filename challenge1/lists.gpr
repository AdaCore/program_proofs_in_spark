with "sparklib";

project Lists is
   package Builder is
      for Global_Configuration_Pragmas use "spark.adc";
   end Builder;

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O0", "-gnatX");
   end Compiler;

end Lists;
